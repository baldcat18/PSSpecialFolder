<Window
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	Height="430" Width="600" ResizeMode="CanResizeWithGrip" FontSize="16"
	Title="Show-SpecialFolder"
>
	<Grid>
		<DataGrid
			Name="dataGrid" AutoGenerateColumns="False" Margin="4"
			IsReadOnly="True" HeadersVisibility="None" SelectionMode="Single"
		>
			<DataGrid.Columns>
				<DataGridTextColumn Header="Data" MinWidth="800">
					<DataGridTextColumn.Binding>
						<MultiBinding StringFormat="{}{0}&#x0A;{1}">
							<Binding Path="Name" />
							<Binding Path="Path" />
						</MultiBinding>
					</DataGridTextColumn.Binding>
				</DataGridTextColumn>
			</DataGrid.Columns>
			<DataGrid.ContextMenu>
				<ContextMenu>
					<MenuItem Header="_Open" Name="open" />
					<MenuItem Header="Path _as copy" Name="copyAsPath" />
					<MenuItem Header="Open Command _Prompt here" Name="cmd" />
					<MenuItem Header="Open Power_Shell here" Name="powershell" />
					<MenuItem Header="Open _Linux shell here" Name="wsl" />
					<MenuItem Header="P_roperties" Name="properties"/>
				</ContextMenu>
			</DataGrid.ContextMenu>
		</DataGrid>
	</Grid>
</Window>
